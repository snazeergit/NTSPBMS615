server.port=9092

spring.application.name=MSProj04-EurekaClient_Provider

eureka.client.service-url.default.zone=http://localhost:8761/eureka

#This will make sure the name in the Status field of the Eureka Server
eureka.instance.instance-id=${spring.application.name}:${random.value}